import React from "react";
import AppointmentBanner from "./AppointmentBanner";

const Appointment = () => {
  return (
    <section>
      <AppointmentBanner />
    </section>
  );
};

export default Appointment;
